# Ghi chú công việc - 27/10/2025

## Tóm tắt các công việc đã hoàn thành

1**Đánh giá hệ thống RAG với Reranking**: Chạy test với 30 câu hỏi, so sánh 3 cấu hình (baseline, rerank, rewrite_rerank)

2 **Chuẩn hóa điểm rerank**: Implement min-max normalization cho rerank scores về khoảng [0,1], giữ lại raw scores để debug
3 **Mở rộng candidate pool**: Tăng retrieval multiplier từ x2 lên x5 (top_k=5 → retrieve 25 candidates trước reranking)
4 **Dọn dẹp code duplication**: Loại bỏ hoàn toàn implementation trùng lặp `bge_local`, chỉ giữ `bge_m3_hf_local` với tên rõ ràng hơn
5 **Cập nhật UI**: Streamline reranker options trong Streamlit (4 options thay vì 5), cải thiện tên hiển thị "BGE v2-m3 Local (HF)"
6 **Validate kết quả**: Xác nhận reranking hoạt động đúng, tìm được thông tin version ở Page 1 revision history (28/05/2024 | 5.0)

**Kết luận**: Hệ thống RAG với reranking hoạt động ổn định, code sạch không trùng lặp, sẵn sàng production.
